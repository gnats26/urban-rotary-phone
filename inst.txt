git log to get which commit
git checkout <commit id>
git checkout -b <branch name> # swtiches to branch_name
git merge <branch name>
git add remote <variable_name> <url>
git fetch <variabele_name> # does not merge only fetch
git pull (fetch and merge)
git push <variable_name> <branch>
